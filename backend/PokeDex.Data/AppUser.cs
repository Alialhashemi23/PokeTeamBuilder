using Microsoft.AspNetCore.Identity;

namespace PokeDex.Data
{
    /// <summary>
    /// Application user. Extend with profile fields (display name, avatar, etc.) as needed.
    /// </summary>
    public class AppUser : IdentityUser
    {
    }
}
